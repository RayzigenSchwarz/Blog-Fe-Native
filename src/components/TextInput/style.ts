import {StyleSheet} from 'react-native';

export const inputStyle = StyleSheet.create({
  defaultTextInputStyle: {
    width: '100%',
    height: 40,
    borderStyle: 'solid',
    padding: 12,
    borderWidth: 2,
    borderColor: 'blue',
  },
});
